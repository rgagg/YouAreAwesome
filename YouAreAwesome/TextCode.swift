//
//  TextCode.swift
//  YouAreAwesome
//
//  Created by Richard Gagg on 10/1/2025.
//


/*
Text("Screen Width: \(screenWidth, specifier: "%.2f")\nScreen Height: \(screenHeight, specifier: "%.2f")")
  .font(.title)
  .minimumScaleFactor(0.5)
  .foregroundStyle(.red)
  .frame(maxWidth: .infinity)

Spacer()


Divider()
  .background(.black)
  .frame(width: .infinity)
  .padding(.vertical)

Rectangle()
  .fill(.indigo)
  .frame(width: screenWidth * 0.75, height: 5)
  .padding(.vertical)

Capsule()
  .fill(Color("BackGround"))
  .frame(width: screenWidth - 20, height: 5)
  .padding(.vertical)
*/
